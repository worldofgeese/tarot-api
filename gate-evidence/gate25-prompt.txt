Architect Lens full review — branch: agent/01kpas/reading-cards-update

Run ONLY Level 4 (verification integrity) and Level 5 (pipeline integrity).
L1/L2/L3 were already reviewed at Gate 0.5 — do not repeat them.

Level 4 — Verification Integrity:
- Do tests verify what the user sees, or only what the code contains?
- Is there a gap between 'tests pass' and 'app works'?
- Are there golden fixtures with deterministic expected outputs?
- For artifact projects: is the generated artifact tested, not just generation code?

Level 5 — Pipeline Integrity:
- Did the worker follow the brief exactly, or improvise?
- Was baseline pinned before work started?
- Is there a before/after comparison of outputs?
- Were previous fixes preserved? Any regressions?

Gate 0.5 concerns (for reference):
## Level 1: Deliverable Truth

**What the user receives:**
- A new `PUT /api/readings/:id` endpoint that allows updating `notes` and/or `spread_type` fields on existing readings
- Partial update capability (can update one or both fields)
- Proper validation and error handling with consistent error shapes
- Returns full updated reading object on success

**End-to-end functionality:** ✅ **WORKS**
- Route implementation is complete with proper validation
- Test file is started and follows correct patterns
- Integration with existing database schema is correct
- Error handling matches existing API patterns

## Level 3: Change Integrity

**Scope boundary:** ✅ **WELL BOUNDED**
- Change is purely additive (new PUT route only)
- Does not modify existing functionality
- Follows established patterns from existing codebase
- Constraints explicitly state "additive only"

**Diff size vs complexity:** ✅ **APPROPRIATE**
- ~100 lines for a CRUD update endpoint with validation is reasonable
- Most code

Judge harnessSuggestion (for reference):
Running judge sample 1/3...
Running judge sample 2/3...
Running judge sample 3/3...

=== Judge Summary ===
Verdicts: PASS PASS PASS
Majority: PASS (100% consensus)
Risk: LOW
Output: /home/node/.openclaw/workspace/memory/judge-reviews/20260416-103222.json
✅ PASS - judge approves.


DIFF (truncated):
diff --git a/.rpi/plans/2026-04-16-reading-cards-update.md b/.rpi/plans/2026-04-16-reading-cards-update.md
new file mode 100644
index 0000000..2fff287
--- /dev/null
+++ b/.rpi/plans/2026-04-16-reading-cards-update.md
@@ -0,0 +1,38 @@
+# Plan: PUT /api/readings/:id — Update Reading
+
+**Date:** 2026-04-16
+**Branch:** agent/01kpas/reading-cards-update
+
+## Goal
+
+Add `PUT /api/readings/:id` endpoint to update notes and/or spread_type on an existing reading.
+
+## Constraints
+
+- Additive only — do not modify existing routes
+- Partial update: only provided fields change, others preserved
+- At least one field required (400 if neither provided)
+- Follow existing error shape: `{ error: "..." }`
+- Return full updated reading object on success
+
+## Acceptance Criteria
+
+- `PUT /api/readings/:id { notes }` → 200 with updated reading
+- `PUT /api/readings/:id { spread_type }` → 200 with updated reading
+- `PUT /api/readings/:id { notes, spread_type }` → 200 with both updated
+- `PUT /api/readings/9999 { notes }` → 404 `{ error: "Reading not found" }`
+- `PUT /api/readings/abc { notes }` → 400 `{ error: "Invalid id" }`
+- `PUT /api/readings/:id { spread_type: "invalid" }` → 400 `{ error: "Invalid spread_type..." }`
+- `PUT /api/readings/:id {}` → 400 `{ error: "At least one field (notes or spread_type) is required" }`
+- notes > 2000 chars → 400
+- All existing 236 tests still pass
+
+## Implementation
+
+Route: `.put("/readings/:id", ...)` in `src/routes/api.ts`
+Query: `UPDATE readings SET field1 = ?, field2 = ? WHERE id = ?`
+Validation: same patterns as POST /api/readings
+
+## Test File
+
+`tests/reading-update.test.ts` using `createApp(":memory:")`
diff --git a/CLAUDE.md b/CLAUDE.md
index 31daaa2..3c95866 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -66,8 +66,8 @@ FJ_EX=/home/node/.openclaw/bin/fj-ex
 
 **Test pattern (mandatory):**
 ```ts
-import { describe, it, expect, beforeAll } from "bun:test";
-import createApp from "../src/index";  // default export — NOT { createApp }
+import { describe, it, expect } from "bun:test";
+import { createApp } from "../src/index";
 
 const app = createApp(":memory:");  // ALWAYS in-memory, never data/tarot.db
 
diff --git a/src/routes/api.ts b/src/routes/api.ts
index a6af8f4..34e4519 100644
--- a/src/routes/api.ts
+++ b/src/routes/api.ts
@@ -691,6 +691,93 @@ export function apiRoutes(db: Database) {
         return { error: "Reading not found" };
       }
 
+      return reading;
+    })
+
+    .put("/readings/:id", ({ params: { id }, body, set }) => {
+      // Validate ID is numeric
+      const validation = validateCardId(id);
+      if (!validation.valid) {
+        set.status = 400;
+        return { error: validation.error };
+      }
+
+      const { spread_type, notes } = body as {
+        spread_type?: string;
+        notes?: string;
+      };
+
+      // At least one field required
+      if (spread_type === undefined && notes === undefined) {
+        set.status = 400;
+        return { error: "At least one field (notes or spread_type) is required" };
+      }
+
+      // Validate spread_type if provided
+      if (spread_type !== undefined) {
+        if (typeof spread_type !== "string") {
+          set.status = 400;
+          return { error: "spread_type must be a string" };
+        }
+        const validSpreadTypes = ["single", "three-card", "celtic-cross", "custom"];
+        if (!validSpreadTypes.includes(spread_type)) {
+          set.status = 400;
+          return { error: "Invalid spread_type. Must be one of: single, three-card, celtic-cross, custom" };
+        }
+      }
+
+      // Validate notes length if provided
+      if (notes !== undefined) {
+        if (typeof notes !== "string") {
+          set.status = 400;
+          return { error: "notes must be a string" };
+        }
+        if (notes.length > 2000) {
+          set.status = 400;
+          return { error: "notes must be 2000 characters or less" };
+        }
+      }
+
+      const numericId =

Verdict on last line: APPROVE / CONCERNS / BLOCK