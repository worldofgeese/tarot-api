schema_version = "1.0.0"

[init]
specs = true
diagram_style = "ascii"
entrypoints = [
    "AGENTS.md",
    "CLAUDE.md",
    "GEMINI.md",
    "CODEX.md",
]

[repo]
product_name = "tarot-api"
product_summary = "A three-tier tarot card application with SQLite database, Elysia API, and server-rendered HTML frontend."
architecture_direction = "Composable repository architecture with explicit boundaries and proof-backed delivery invariants."
product_type = "service_or_library"
done_criteria = "Decapod validate passes, required tests pass, and promotion-relevant artifacts are present."
primary_languages = ["typescript/javascript"]
detected_surfaces = ["npm"]
